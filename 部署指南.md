# 部署到 Railway 指南（水闸设计论文生成系统）

## 一、准备（一次性）

### 1. 把项目推到一个 GitHub 仓库

在项目目录执行（如未初始化过 git）：

```bash
cd C:/Users/24811/WorkBuddy/2026-07-25-22-15-38
git init
git add .
git commit -m "水闸设计系统：多用户登录 + 工程图纸 + 论文生成"
```

然后在 GitHub 建一个仓库，把代码推上去：

```bash
git remote add origin https://github.com/你的用户名/sluice-design.git
git branch -M main
git push -u origin main
```

### 2. 注册/登录 Railway

打开 https://railway.app 用 GitHub 账号登录（之前用过的话直接登录）。

## 二、部署（约 5 分钟）

### 方式 A：GitHub 自动部署（推荐，最省事）

1. Railway 控制台点 **+ New Project**
2. 选 **Deploy from GitHub repo** → 选你刚推的 `sluice-design` 仓库
3. Railway 自动检测 `Procfile` + `requirements.txt`，开始构建部署
4. 部署完成后点生成的域名即可访问

### 方式 B：CLI 部署（若想用命令行）

```bash
# 安装 Railway CLI（Windows 用 PowerShell 或 npm）
npm install -g @railway/cli
railway login   # 浏览器授权
railway init    # 关联项目
railway up      # 上传部署
```

## 三、关键配置（必须做！否则数据会丢）

### 1. 创建数据持久卷（Volume）

文件系统在 Railway 重启后会被清空，**必须用 Volume 保存数据库和图纸**：

1. 在 Railway 项目里打开你的服务 → **Variables** 页
2. 添加变量：
   - `DATA_DIR` = `/app/data`（数据目录指向挂载点）
   - `SECRET_KEY` = 随便一长串随机字符（如 `a1b2c3...`）
3. 打开 **Volumes** 页 → **Add Volume**
   - Mount Path 填 `/app/data`
   - 容量默认 1GB 够用

添加后会自动重启，数据库、注册用户、生成的图纸都会持久保存。

### 2. 生成公网域名

- 服务 → **Settings** → **Networking** → **Generate Domain**
- 得到一个 `https://xxxx.up.railway.app` 公网地址，任何地方都能登录使用

## 四、验证

打开公网域名 → 注册账号 → 登录 → 填参数 → 生成图纸 / 生成论文 → 下载 DXF。

## 五、本地启动（不部署时）

```bash
cd C:/Users/24811/WorkBuddy/2026-07-25-22-15-38
C:/Users/24811/.workbuddy/binaries/python/envs/default/Scripts/python.exe server.py
# 访问 http://localhost:5001
```
